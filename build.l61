function main(argc, argv)
    s = l61.getPwd()
    print(type(s))
    print(s)

   if mountLib("test").test() == "test" then
        print(mountLib("test").test())
   end

   for _, value in pairs(argv) do
       print(value.."\n")
   end

   for _, value in pairs(spaths) do
          print(value.."\n")
   end
   --print(bash.."\n")
   --while true do
   --end
   l61.pushEventBus()
   l61.fs.exists("/bin")
   return 1
end

--return 0